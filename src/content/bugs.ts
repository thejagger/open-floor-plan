import type { BugStats } from '../sim/entities';

/** Placeholder combat values — replaced by Milestone 2's balance harness. */
export const TYPO: BugStats = { type: 'typo', hp: 2, speed: 1.5, leakCost: 1 };
