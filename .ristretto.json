{
  "gates": {
    "format": "",
    "lint": "node -e \"process.exit(require('fs').existsSync('package.json')?1:0)\" || npm run lint",
    "typecheck": "node -e \"process.exit(require('fs').existsSync('tsconfig.json')?1:0)\" || npx tsc --noEmit",
    "test": "node -e \"process.exit(require('fs').existsSync('package.json')?1:0)\" || npm test",
    "testChanged": ""
  }
}
